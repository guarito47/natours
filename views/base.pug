doctype html 
html
  head 
    block head
      meta(charset='UTF-8')
      meta(name='viewport' content='width=device-width, initial-scale=1.0')    
      link(rel='stylesheet', href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
      link(rel='stylesheet', href='/css/style.css')
      link(rel='shortcut icon', type='image/png' href='/img/favicon.png')      
      title Natours | #{tittle}    
      

  body 
    //- HEADER SECTION
    include _header
    //- CONTENT SECTION
    block content
      h1 Welcome to Natours!
    //- FOOTER SECTION
    include _footer   
    //-as we comment this lines because now we use parcel bundler that works for mapbox and login but not
    //-for axios, thats why we will install as npm package
    //-script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/1.10.0/axios.min.js')
    //-script(src='/js/mapbox.js')
    //-script(src='/js/login.js')       
    script(src='https://js.stripe.com/basil/stripe.js')
    script(src='/js/bundle.js')
    